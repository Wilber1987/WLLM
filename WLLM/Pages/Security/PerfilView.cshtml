@page
@using API.Controllers
@{
    // Layout = null;
    if (!SecurityController.Auth(HttpContext.Session.GetString("sessionKey")))
    {
        Response.Redirect("../Security/login");
        return;
    }
    if (!SecurityController.HavePermission("PERFIL_ACCESS", HttpContext.Session.GetString("sessionKey")))
    {
        Response.Redirect("../Security/permission_error");
        return;
    }
}

<script src="/Gestion_Perfil/ViewProfile.js" type="module"></script>